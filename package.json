{
  "name": "auto-tracking-study-work-timer",
  "version": "0.1.0",
  "private": true,
  "description": "Local study and work timer with Google Sheets logging.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "googleapis": "^157.0.0"
  }
}
